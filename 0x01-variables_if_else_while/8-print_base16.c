#include <stdio.h>

/**
 * main - entry file
 * description: print numbers in hex
 * Return: 0 Always success
 */

int main(void)
{
	int i;
	char k;

	for (i = 48; i < 58; i++)
		putchar(i);

	for (k = 'a'; k <= f; k++)
		putchar(k);

	putchar('\n');
	return (0);
}
