#include "main.h"

/**
 * _islower - function checks for lowercase characters
 * @i: the character to check
 * Return: 1 (success), 0 (fail)
 */

int _islower(int i)
{
	return (i >= 'a' && i <= 'z');
}

